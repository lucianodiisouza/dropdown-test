import styled from "styled-components";
import { colors } from "../../theme";

export const Container = styled.button`
  width: 48px;
  height: 48px;

  background-color: ${colors.background.black700};
  border: none;
`;
